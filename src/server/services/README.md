# Server services

All authoritative financial writes and admin operations belong in this server-only boundary. Browser Supabase clients must never write balances, transactions, earnings, approval statuses, audit logs, or admin records.
