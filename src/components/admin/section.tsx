import type { ReactNode } from "react";
import { PageTransition } from "@/components/motion/motion-system";
export function AdminSection({ title, description, children }: { title: string; description: string; children: ReactNode }) { return <PageTransition><header className="mb-9 border-b border-border/80 pb-7"><p className="font-reference text-[11px] font-semibold uppercase tracking-[0.2em] text-gold">Admin control centre / AAL2</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.055em] sm:text-5xl">{title}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-muted">{description}</p></header>{children}</PageTransition>; }
