import { Alert } from "@/components/ui/alert";
import { PageHero, PageShell, PublicSection } from "@/components/public/page-shell";

export type LegalSection = { title: string; paragraphs?: readonly string[]; points?: readonly string[] };
export function LegalPage({ eyebrow, title, description, sections }: { eyebrow: string; title: string; description: string; sections: readonly LegalSection[] }) { return <PageShell><PageHero eyebrow={eyebrow} title={title} description={description} /><PublicSection title="Important context"><Alert tone="warning" title="Interim public summary">Final company information, governing jurisdiction, and approved legal text are still awaiting confirmation. This page explains the intended approach and is not a substitute for final legal documents.</Alert><article className="mt-12 max-w-3xl divide-y divide-border">{sections.map((section) => <section className="py-8 first:pt-0" key={section.title}><h2 className="text-2xl font-semibold">{section.title}</h2>{section.paragraphs?.map((paragraph) => <p className="mt-4 leading-7 text-muted" key={paragraph}>{paragraph}</p>)}{section.points && <ul className="mt-4 grid gap-3 text-muted">{section.points.map((point) => <li className="flex gap-3 leading-7" key={point}><span aria-hidden className="mt-3 size-1.5 shrink-0 rounded-full bg-emerald" />{point}</li>)}</ul>}</section>)}</article></PublicSection></PageShell>; }
