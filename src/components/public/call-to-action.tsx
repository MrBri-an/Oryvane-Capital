import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { buttonVariants } from "@/components/ui/button";
export function CallToAction({ title = "Build your understanding first.", description = "Explore the platform approach, review the risks, and create an account when you are ready to continue." }: { title?: string; description?: string }) { return <section className="px-[var(--space-page)] py-16 sm:py-24"><div className="mx-auto max-w-[90rem] overflow-hidden rounded-2xl border border-emerald/25 bg-surface p-6 shadow-md sm:p-10 lg:p-14"><h2 className="max-w-3xl text-3xl font-semibold tracking-[-0.035em] sm:text-5xl">{title}</h2><p className="mt-5 max-w-2xl leading-7 text-muted">{description}</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/register" className={buttonVariants({ size: "lg" })}>Create account <ArrowRight aria-hidden className="size-4" /></Link><Link href="/login" className={buttonVariants({ variant: "secondary", size: "lg" })}>Log in</Link></div></div></section>; }
