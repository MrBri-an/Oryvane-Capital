import type { ReactNode } from "react";
export function Tooltip({ label, children }: { label: string; children: ReactNode }) { return <span className="group relative inline-flex"><span aria-describedby={`tooltip-${label.replace(/\s+/g, "-")}`}>{children}</span><span id={`tooltip-${label.replace(/\s+/g, "-")}`} role="tooltip" className="pointer-events-none absolute bottom-[calc(100%+0.5rem)] left-1/2 z-[var(--z-overlay)] w-max max-w-56 -translate-x-1/2 rounded-sm bg-foreground px-2 py-1 text-xs text-background opacity-0 shadow-md transition-opacity group-focus-within:opacity-100 group-hover:opacity-100">{label}</span></span>; }
