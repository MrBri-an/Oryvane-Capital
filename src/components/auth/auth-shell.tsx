import Link from "next/link";
import type { ReactNode } from "react";
import { PageTransition } from "@/components/motion/motion-system";
import { MarketUniverse } from "@/components/market/market-universe";

export function AuthShell({ eyebrow, title, description, children, footer }: { eyebrow: string; title: string; description: string; children: ReactNode; footer?: ReactNode }) { return <PageTransition><main className="relative grid min-h-screen overflow-hidden lg:grid-cols-[minmax(22rem,.82fr)_1.18fr]"><MarketUniverse/><section className="relative z-10 flex items-center justify-center border-r border-border/70 bg-background/88 px-4 py-10 backdrop-blur-xl sm:px-8"><div className="w-full max-w-md"><Link href="/" className="inline-flex min-h-11 items-center gap-2 rounded-sm font-heading text-lg font-bold"><span aria-hidden className="grid size-8 place-items-center rounded-full border border-emerald/50 bg-emerald/10 text-xs text-emerald-strong">O</span>Oryvane Capital</Link><p className="mt-12 font-reference text-[11px] font-semibold uppercase tracking-[0.22em] text-cyan">{eyebrow}</p><h1 className="mt-4 text-4xl font-semibold leading-[1] tracking-[-0.055em] sm:text-5xl">{title}</h1><p className="mt-4 text-sm leading-6 text-muted">{description}</p><div className="mt-9 rounded-2xl border border-border bg-surface/80 p-5 shadow-[0_30px_90px_rgb(0_0_0/.4)] sm:p-7">{children}</div>{footer && <div className="mt-7 text-center text-sm text-muted">{footer}</div>}</div></section><aside aria-hidden className="relative hidden p-12 lg:flex lg:items-end"><div className="relative max-w-xl"><p className="font-reference text-xs text-emerald-strong">SECURE SESSION / CONTROLLED ACCESS</p><p className="mt-6 font-heading text-5xl font-semibold leading-[1.02] tracking-[-0.055em]">The quiet side of financial confidence.</p><p className="mt-6 max-w-md leading-7 text-muted">Cookie-based sessions, server verification, and separated administrative assurance keep authority where it belongs.</p><p className="mt-8 font-reference text-xs text-warning">NEVER SHARE PASSWORDS OR VERIFICATION CODES</p></div></aside></main></PageTransition>; }
