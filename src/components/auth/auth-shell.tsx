import Link from "next/link";
import type { ReactNode } from "react";
import { PageTransition } from "@/components/motion/motion-system";

export function AuthShell({ eyebrow, title, description, children, footer }: { eyebrow: string; title: string; description: string; children: ReactNode; footer?: ReactNode }) { return <PageTransition><main className="grid min-h-screen lg:grid-cols-[minmax(20rem,0.85fr)_1.15fr]"><section className="flex items-center justify-center px-4 py-10 sm:px-8"><div className="w-full max-w-md"><Link href="/" className="inline-flex min-h-11 items-center gap-2 rounded-sm font-heading text-lg font-bold"><span aria-hidden className="grid size-7 place-items-center rounded-full border border-gold/60 text-xs text-gold">O</span>Oryvane Capital</Link><p className="mt-10 text-xs font-semibold uppercase tracking-[0.18em] text-gold">{eyebrow}</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.04em] sm:text-4xl">{title}</h1><p className="mt-3 text-sm leading-6 text-muted">{description}</p><div className="mt-8">{children}</div>{footer && <div className="mt-7 text-center text-sm text-muted">{footer}</div>}</div></section><aside aria-hidden className="hidden border-l border-border bg-surface-subtle p-10 lg:flex lg:items-end"><div className="max-w-lg"><p className="font-heading text-4xl font-semibold tracking-[-0.04em]">Security is part of the experience.</p><p className="mt-5 leading-7 text-muted">Cookie-based sessions, server verification, and deliberately separated administrative access form the authentication foundation.</p><p className="mt-6 text-sm font-semibold text-warning">Never share your password or verification code.</p></div></aside></main></PageTransition>; }
