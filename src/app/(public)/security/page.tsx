import type { Metadata } from "next";
import { Fingerprint, History, KeyRound, LockKeyhole, ServerCog, UserRoundCheck } from "lucide-react";
import { FeatureGrid } from "@/components/public/feature-grid";
import { PageHero, PageShell, PublicSection } from "@/components/public/page-shell";
import { Alert } from "@/components/ui/alert";
export const metadata: Metadata = { title: "Security approach", description: "Explore the security principles planned for Oryvane Capital, including server-side control, separate admin access, and accountable records." };
export default function SecurityPage() { return <PageShell><PageHero eyebrow="Security" title="Designed around boundaries, not assumptions." description="Oryvane Capital's security foundation separates public interfaces, user access, administrative authority, and protected financial operations." /><PublicSection title="Defence through clear responsibility"><FeatureGrid items={[{ icon: ServerCog, title: "Server-side authority", description: "Sensitive financial and administrative operations are intended to validate identity and permission on trusted server boundaries." }, { icon: UserRoundCheck, title: "Separated access", description: "Normal user and administrator routes remain distinct, and hidden interface controls are never treated as authorisation." }, { icon: KeyRound, title: "Stronger admin assurance", description: "The approved design requires administrator MFA, shorter sessions, and reauthentication for sensitive actions." }, { icon: LockKeyhole, title: "Private information", description: "Sensitive payment evidence is intended for private storage with authorised, time-limited access." }, { icon: History, title: "Durable audit history", description: "Material administrative and financial actions are designed to create permanent records that cannot be silently removed." }, { icon: Fingerprint, title: "Least privilege", description: "Administrative capabilities are intended to follow specific roles and permissions rather than blanket access." }]} /><Alert className="mt-10" title="Security implementation continues in later phases">Authentication, database policies, protected storage, and security testing are not represented as complete on this public page.</Alert></PublicSection></PageShell>; }
